apiVersion: v1
kind: Service
metadata:
  name: {{ include "jinguzhou.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app.kubernetes.io/name: {{ include "jinguzhou.name" . }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
